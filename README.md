# README

## How to run backend

`bundle install`

`rails db:create`

`rails db:migrate`

`rails s`

## How to run frontend

`cd front`

`npm install`

`npm start`

Then the app it's gonna run at `localhost:3000`
